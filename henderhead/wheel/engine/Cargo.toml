[package]
name = "waterwheel"
version = "0.1.0"
edition = "2021"
description = "A leaky waterwheel, its first-harmonic reduction, and the Lorenz system they are both"

[lib]
crate-type = ["cdylib", "rlib"]

[profile.release]
opt-level = 3
lto = true
panic = "abort"
codegen-units = 1
