[package]
name = "craftca"
version = "0.1.0"
edition = "2021"
description = "A cellular automaton whose transition rule is a table of crafting recipes"

[lib]
crate-type = ["cdylib", "rlib"]

[profile.release]
opt-level = 3
lto = true
panic = "abort"
codegen-units = 1
