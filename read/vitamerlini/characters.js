/* Vita Merlini — Layer 3: character cards + web.
   Skeleton only for now: the Read layer (the original translation) is being
   written movement by movement first; the cast is filled in once the
   translation is far enough along to fix each figure's appearances and
   relationships. Attaches to window.VITAMERLINI.characters.
   Shape: { intro, roles:[{id,label,color}], cast:[{id,name,role,blurb,appears,rel}] }. */
window.VITAMERLINI = window.VITAMERLINI || {};
window.VITAMERLINI.characters = {
  intro: "The cast of the <em>Vita Merlini</em> is in preparation. The Read layer — the original translation from the Latin — comes first, movement by movement; the character cards and the force-laid web are written once the translation has fixed each figure's appearances and ties.",
  roles: [],
  cast: [],
};
