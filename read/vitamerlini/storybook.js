/* Vita Merlini — Layer 7: a faithful storybook retelling. Skeleton only for
   now; written last, after the deeper layers, so the page-by-page retelling
   can be checked against them. Attaches to window.VITAMERLINI.book.
   Shape: { meta:{kicker,note}, spreads:[{title,sub?,text,illus}] }. */
window.VITAMERLINI = window.VITAMERLINI || {};
window.VITAMERLINI.book = {
  meta: { kicker: "Vita Merlini", note: "The storybook retelling is in preparation — it is written last, once the translation and the annotation layers are in place." },
  spreads: [],
};
