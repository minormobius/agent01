name: Build cylinder solver (wasm)

# Compiles hoop/solver/cylinder-solver-wasm to WASM with wasm-pack and commits the
# result to hoop/solver/pkg/. The cylinder tool (hoop/cylinder.html) loads it as an
# OPTIONAL accelerator with a JS fallback, so the page works whether or not this has
# run. Committing hoop/solver/pkg/** lands under hoop/**, which deploy-hoop serves;
# a GITHUB_TOKEN push doesn't trigger other workflows, so we dispatch deploy-hoop
# explicitly. The trigger is scoped to the Rust SOURCE (not pkg/), so there's no loop.

on:
  push:
    branches:
      - main
      - 'claude/merge-features-update-endpoints-qa2vu1'
    paths:
      - 'hoop/solver/cylinder-solver/**'
      - 'hoop/solver/cylinder-solver-wasm/**'
      - '.github/workflows/build-cylinder-solver.yml'
  workflow_dispatch:

permissions:
  contents: write
  actions: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run native solver tests (the math, offline)
        working-directory: hoop/solver/cylinder-solver
        run: cargo test --release

      - name: Add wasm32 target
        run: rustup target add wasm32-unknown-unknown

      - name: Cache cargo + target
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            hoop/solver/cylinder-solver-wasm/target
          key: cargo-cylinder-${{ runner.os }}-${{ hashFiles('hoop/solver/**/Cargo.toml', 'hoop/solver/**/src/**') }}
          restore-keys: cargo-cylinder-${{ runner.os }}-

      - name: Install wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh

      - name: Build Rust/WASM solver
        working-directory: hoop/solver/cylinder-solver-wasm
        run: wasm-pack build --release --target web --out-dir ../pkg --out-name cylinder_solver

      - name: Commit pkg/
        id: commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          rm -f hoop/solver/pkg/.gitignore hoop/solver/pkg/package.json hoop/solver/pkg/README.md
          git add -f hoop/solver/pkg
          git commit -m "build: cylinder solver wasm" || { echo "changed=false" >> "$GITHUB_OUTPUT"; exit 0; }
          git push
          echo "changed=true" >> "$GITHUB_OUTPUT"

      # GITHUB_TOKEN pushes don't trigger other workflows, so kick deploy-hoop
      # explicitly to publish the freshly-built pkg/.
      - name: Deploy (dispatch deploy-hoop)
        if: steps.commit.outputs.changed == 'true'
        run: |
          curl -s -X POST \
            -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${{ github.repository }}/actions/workflows/deploy-hoop.yml/dispatches" \
            -d '{"ref":"${{ github.ref_name }}"}' || echo "dispatch skipped (non-fatal)"
