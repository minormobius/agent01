{
  "$schema": "node_modules/wrangler/config-schema.json",
  // GOLDEN RULE: `name` must be the worker that owns alph.minomobi.com, and the
  // domain must appear in `routes` — otherwise wrangler updates a stray
  // alph.workers.dev and the live site never changes. Verify the deploy log
  // binds "alph.minomobi.com (custom domain)".
  "name": "alph",
  "compatibility_date": "2026-02-20",
  "assets": {
    "directory": "."
  },
  "routes": [
    { "pattern": "alph.minomobi.com", "custom_domain": true }
  ]
}
