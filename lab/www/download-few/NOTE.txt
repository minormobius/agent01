No network again, so the poly.pizza links were out of reach. Even with one, this page's CSP blocks live fetches to other hosts — a model would need baking in at build time. Added a checkerboard cube instead, for clearer UV mapping.