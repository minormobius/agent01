Still no network tool this turn, so the opengameart link is blocked too — same limit, not the link. Added a fifth shape (Drum), built from a generated ring instead of typed coordinates — the technique real curved geometry would need.