That link points straight at the raw .obj file, skipping the page that lists its licence, so the downloader can't verify it and silently skips it. The item's own opengameart.org page (the one showing "License(s):") would actually go through.