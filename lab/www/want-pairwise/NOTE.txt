Found it: the crash was a typo, not the pfp problem — a lookup table used key 'both' but nodes are tagged 'shared', so every compare threw before the diagram could render. Fixed, one line. The pfp/CORS question is untouched, that wasn't this bug.
