Copy now writes two clipboard formats at once: the flat PNG (real photos only if Bluesky's CDN allows it, else initials) plus a richer paste with real photos that shows up in Docs/Slack/Mail — not in a plain-image paste target.
