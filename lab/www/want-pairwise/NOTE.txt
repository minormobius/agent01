The referrer theory was a dead end — this page already forces no-referrer site-wide, so that was never the fix. Added a second button instead: real photos via right-click Copy Image, sidestepping the CORS wall rather than re-guessing it.
