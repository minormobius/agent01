Found it: the diagram used a blob: URL, but the domain CSP has no blob: in img-src, so it silently failed to show. Copy worked fine (clipboard skips that check) — you just never saw proof. Switched to data: URI, should render now.
