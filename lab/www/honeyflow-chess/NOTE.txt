Found the actual bug: flow was injected into one grid cell, so a neighbor square never got enough of it — not weak, just unreachable. Now it splats wide and hits neighbors directly. Strength is still a guess pending a real run.
