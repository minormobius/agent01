No jump — left/right only, per the ask. Gravity already means edge grade here, so a future jump should cross a too-steep edge at a cost, not be generic physics. Collision is free: nearest-node lookup, same as the diagram.