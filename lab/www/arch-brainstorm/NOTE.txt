Local view drops source/sink colour + path highlight on purpose — read "no global landscape info" as more than zoomed in. One-line revert if you meant plain zoom. Also killed button-hold text-select on every button, not just move.