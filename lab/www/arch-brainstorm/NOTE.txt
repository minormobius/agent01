Grade is each Node's height vs its neighbour (rise/run), not the wall's angle — stable as you edit nearby cells. Slider sets the threshold; amber = open but too steep. Still graph-only, not a sprite walking yet — that's next.