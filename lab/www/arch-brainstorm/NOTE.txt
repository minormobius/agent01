Fair catch — the math was right (grade = rise/run between two Nodes' heights = the floor's own slope), but every label blamed the Edge. Reworded to say floor, and made floor height glow across each cell, not just the dot.