Did the three perf fixes: resize is debounced and skips the nebula-tile rebuild when the screen's max dimension hasn't changed, and word glow is now a cached sprite instead of a shadowBlur redrawn every frame. Nothing visible changes.
