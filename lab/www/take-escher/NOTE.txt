Found it: recentring was quietly stamping a fresh, unrelated patch of tiles into the buffer instead of reusing the real ones — that's the half-pitch mess. Replaced it with a lossless re-numbering of the same live tiles, no new geometry invented. Couldn't test in a browser this turn.