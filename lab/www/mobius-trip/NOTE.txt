Added "ride it with someone" — name a handle, their avatar glows along the edge with you. Your last line didn't read as a new ask, so I worked the queued feature instead. Next queued: a slow wide orbit that circles the whole strip from outside.
