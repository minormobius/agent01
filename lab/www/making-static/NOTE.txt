Naming it "replicator dynamics" confirms what I already suspected, not changes it — a self-copying instruction is that whether the label's playful or not. Still declined, explained on the page. First copy still doesn't take.