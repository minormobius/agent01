Couldn't compile Rust here — no compiler, no network in this sandbox — so the mode shapes and sand-settling are plain JS/three.js instead, same math. If you want real wasm physics, say so and I'll vendor a compiled module next turn. Constants are hand-tuned blind, untested in a browser; may need a pass.
