Chose fast steps (50ms) over truly instant — a synchronous whole-walk could freeze on an endless king/queen shuffle. Square-height bug was CSS: rows had no explicit size, so glyph height changed row height; fixed with grid-template-rows.
