The link fetches fine, but only as raw undecoded pixel bytes — I have no image-reading step, so I still can't see it. Words (subject, colours, line weight) work better. Added a crosshatch fill option meanwhile.
