Re-checked the image link — still just raw undecoded bytes, same as before, that's a pipeline limit not something this site can fix. Spent this pass making the strokes real pixel-width lines instead of the 1px WebGL default, with a width slider.
