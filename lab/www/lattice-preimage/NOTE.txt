Found it: the auto-fit zoom had a floor that stopped it zooming out far enough for a deep tree's farthest points, so they were computed and drawn correctly but just off-screen. Floor now adapts to how far each run actually spreads.
