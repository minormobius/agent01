Both were real caps, not last time's zoom bug. Node cap 3000→20000 (the actual points-shown limit); depth field 10→1000, since the node cap was always the true ceiling. Untested at full size — if a huge tree pans laggy, that's next.
