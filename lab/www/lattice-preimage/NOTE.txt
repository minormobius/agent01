Added "grow tree from origin": fresh random matrix per newly-linked point, recursive. Branching is up to 4x/level, so it hits a 3000-node safety cap before level 8 in practice — the readout says so rather than hanging your tab or faking the depth.