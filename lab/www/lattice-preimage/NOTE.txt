Both added, in tree controls. Also made your aside checkable, not just true: the readout now counts how many edges exceed what a plain tree would have, so a degree-coloured run shows exactly where branches merged back together.
