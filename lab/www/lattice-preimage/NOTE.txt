Matrix is now shared per level, not per point — much less tangled. Zoom widened a lot (0.02x-600x) so grow+fit should show the whole tree; if it still clips, that's the 3000-node safety cap, not the zoom.