Fixed the ranking bug: it only ranked mutuals past the slider, so most accounts got getFollows order. The empty-grid one I couldn't reproduce with no network — likely rate-limiting; now retried, with a warning if it happens again.
