Cap now goes to 100, and top means ranked by how often you reply to each mutual (sampled from ~300 recent posts), not just whoever getFollows lists first. Defaults to 50 since 100 means ~100 feed scans — push it up for the full run.
