Found it: push committed on the raw tap, and a tap near a tile's centre is a coin-flip between two direction buckets — that's the "rotates". Push is now press-drag-release with a live preview arrow, plus much bolder arrowheads everywhere.