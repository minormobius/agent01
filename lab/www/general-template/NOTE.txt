Made the pondertag div itself louder rather than adding a second reminder elsewhere: a warning marker, "required, not optional," and an explicit "do not delete" line, plus a matching bullet in the mobile checklist since that's the section most likely read as a copy-paste punch list.