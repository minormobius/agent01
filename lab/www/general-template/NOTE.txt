No — a build's diff is walled off to this one directory, so I can't add a pointer in root CLAUDE.md myself. Explained why on the page. Closing that gap needs a human edit outside a lab turn, not a page declaring itself required reading.