Read "Interesting" as a reaction, not a new ask, so I reread the whole page plus the pds.js backend for a real bug instead of guessing a feature — found none this time. Everything left in the plan needs a specific ask to build safely.