Fair — read that as chatter too, but it prompted a closer look instead of a new feature. Found a real bug while in there: a returning signed-in visitor who finished the race fast could have their score silently never post. Fixed. Nothing visibly new this turn.