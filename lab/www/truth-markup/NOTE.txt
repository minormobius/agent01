Gave the chart one shared log axis across all five assets instead of each line filling its own band, so absolute scale is finally comparable — NAND now sits low and flat because it genuinely is tiny next to HBM, not a bug.
