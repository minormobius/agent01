That read as a reaction, not a new ask, so this turn double-checked the code instead of guessing a feature (found nothing broken). If there's a specific thing you want next -- the ninja fx, a second market, a downloadable animation -- just name it.