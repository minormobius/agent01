Read "comparted into an imaging mover" as: turn the tiles into one downloadable image. Added a "Save gallery as one image" button — stitches the tiles into a single PNG. Meant an animated/moving image instead? Say so and that's the next build.
