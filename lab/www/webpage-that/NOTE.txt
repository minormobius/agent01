Every word on the page is set in the static font now — labels, buttons, filter pills, entry text, even the crumb up top. One holdout: the box you type into stays plain so you can still see what you're typing; the echo below it renders in the font.