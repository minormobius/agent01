Can't ship an actual font file from here — no compiler, no network. Instead there's a box: type into it and your own words render live in the noise, plus an A-Z/a-z/0-9 strip below it in the same treatment.