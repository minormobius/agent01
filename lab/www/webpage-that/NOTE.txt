Took "static" as the word itself, not the site format — amber and fur through TV snow to CSS. Thirteen entries, filterable by kind. The noise canvas is real per-pixel randomness you can freeze, which is the joke: it's the one static that doesn't move.
