"Horizontal scaling" was ambiguous, so I picked: it scales only Re(eta) in eta=(z-c0)+1/(z-c0), not the pre-image circle. Pixels are pulled back through the closed-form inverse (exterior root), then f iterates from there — exact transform, no gaps.
