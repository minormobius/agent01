# @modulomino.bsky.social

## Palette and type
No stated preference yet — sites built so far use the kit defaults unmodified
(amber accent, dark background, monospace throughout).

## Layout
No stated preference yet.

## Features they reach for
Utility, single-purpose tools rather than content/feed pages: `tzclock` (a
UTC clock with copy-to-ISO-8601) has no Bluesky content at all, unlike
earlier requests. Likes a copy-to-clipboard action on the thing the page
produces.

## Said no to
Nothing recorded yet.

## Notes
`tzclock` was asked for twice ("try again") — the first attempt never left a
directory behind, so there's no evidence of what was wrong with it beyond
the bare request. If a third round comes back with specific complaints,
record them here.
