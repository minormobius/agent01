# @jauntywunderkind.bsky.social

## Requests so far
Asked for atproto login + save/restore/share on a rail-building game
(quoted from @minormobius.bsky.social's train-game description). Posted as
a half-joking "I'm going to be so rich" / "I was mostly joke posting
assuming I had no powers" — reads as delighted rather than demanding; low-
stakes tone, comfortable with an honest "here's what shipped and what
didn't" answer rather than needing everything to work perfectly.

Later asked for an implementation of seam carving (content-aware image
resizing) after reading the original Avidan & Shamir paper, and explicitly
asked whether the paper could actually be read first — cares about the
build being a faithful implementation of a specific known algorithm, not
just a vibe in the same direction. Comfortable with an honest caveat when a
source couldn't literally be fetched (no network tools in the sandbox) as
long as the algorithm itself is implemented correctly from what's actually
well-documented public knowledge.

## Features they reach for
Real backend integration (atproto login, save-to-PDS, share links) when the
request calls for persistent/shareable state — but also just wants a
correct, faithful implementation of an algorithm/paper as a pure front-end
demo when that's the actual ask; not every request needs a backend.

## Said no to
Nothing yet.
